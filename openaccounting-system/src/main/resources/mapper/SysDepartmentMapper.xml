<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qoobot.openaccounting.system.mapper.SysDepartmentMapper">

    <resultMap id="BaseResultMap" type="com.qoobot.openaccounting.system.entity.SysDepartment">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="department_name" property="departmentName"/>
        <result column="department_code" property="departmentCode"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
    </resultMap>

    <resultMap id="VOResultMap" type="com.qoobot.openaccounting.system.vo.SysDepartmentVO" extends="BaseResultMap">
        <result column="parent_name" property="parentName"/>
    </resultMap>

    <select id="selectDepartmentPage" resultMap="VOResultMap">
        SELECT
            d.*,
            p.department_name AS parent_name
        FROM sys_department d
        LEFT JOIN sys_department p ON d.parent_id = p.id
        <where>
            <if test="departmentName != null and departmentName != ''">
                AND d.department_name LIKE CONCAT('%', #{departmentName}, '%')
            </if>
            <if test="status != null">
                AND d.status = #{status}
            </if>
        </where>
        ORDER BY d.sort ASC, d.create_time DESC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM sys_department WHERE parent_id = #{parentId} ORDER BY sort ASC
    </select>

</mapper>
